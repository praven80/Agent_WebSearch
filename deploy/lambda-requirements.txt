# Runtime dependencies for the Lambda-hosted API.
#
# Installed with manylinux wheels for the Lambda runtime, not the local mac venv.
#
# boto3 AND botocore are both pinned deliberately. Something in the dependency
# tree pulls botocore in regardless, and a bundled botocore would shadow the
# runtime's copy while boto3 still came from the runtime -- a mismatched pair.
# Bundling both keeps them consistent and matches the versions verified locally.

fastapi==0.141.1
mangum==0.19.0
strands-agents==1.55.1
mcp==1.30.0
mcp-proxy-for-aws==1.7.0
boto3==1.43.94
botocore==1.43.94
